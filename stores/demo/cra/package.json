{
  "name": "linex-stores-demo-cra",
  "scripts": {
    "start": "npm run copy:src && react-scripts start",
    "build": "npm run copy:src && react-scripts build",
    "copy:src": "cp -a ../src .",
    "local": "npm install --no-save $(npm pack ../../.. | tail -1) $(npm pack ../.. | tail -1)"
  },
  "dependencies": {
    "@babel/polyfill": "^7.10.4",
    "linex": "^2.0.1",
    "linex-stores": "^1.0.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "ie 11",
    "not ie < 11",
    "not op_mini all"
  ]
}
