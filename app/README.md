# Linex Example React App

Bootstrapped with `create-react-app`. Run `npm i` and then `npm start` to open
the app. It's necessary to first run a build in the main directory with
`npm i` and `npm run build`.
